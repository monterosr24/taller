import { Component } from '@angular/core';
@Component({
    selector: 'app-payment-dialog',
    template: `<h2 mat-dialog-title>Add Payment</h2><p>Dialog form to add payment to an invoice with amount, date, method, reference.</p>`
})
export class PaymentDialogComponent { }
